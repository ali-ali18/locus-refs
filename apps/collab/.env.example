# PostgreSQL (mesmo do web)
DATABASE_URL=postgresql://user:password@localhost:5432/refstash

# Mesmo secret do web e do tldraw-sync worker
# Gera um novo: `openssl rand -base64 32`
COLLAB_JWT_SECRET=

# Porta do Hocuspocus
PORT=1234
